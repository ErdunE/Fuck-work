/**
 * Background service worker for FuckWork extension.
 * 
 * Placeholder for Stage 1 build verification.
 */

console.log('FuckWork service worker initialized');

// Message listener placeholder
chrome.runtime.onMessage.addListener((message, sender, sendResponse) => {
  console.log('Received message:', message.type);
  return true;
});



